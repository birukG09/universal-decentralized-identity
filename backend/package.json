{
  "name": "did-vault-contracts",
  "version": "0.1.0",
  "devDependencies": {
    "hardhat": "^2.12.0",
    "@nomiclabs/hardhat-ethers": "^2.2.0",
    "ethers": "^5.7.0",
    "@openzeppelin/contracts": "^4.9.0"
  },
  "scripts": {
    "compile": "hardhat compile",
    "test": "hardhat test"
  }
}